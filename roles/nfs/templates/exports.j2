# Managed by Ansible - do not edit
{% for export in nfs_exports %}
{% set export_path = export.path | default(nfs_export_base ~ '/' ~ export.name) %}
{% set clients = export.clients | default([]) %}
{% if export_path and clients | length > 0 -%}
{{ export_path }} {% for client in clients %}{% set base_opts = client.options | default('rw,sync,root_squash,no_subtree_check') %}{% set opts = base_opts %}{% if export.fsid is defined %}{% set opts = opts ~ ',fsid=' ~ export.fsid %}{% endif %}{{ client.host }}({{ opts }}){% if not loop.last %} {% endif %}{% endfor %}
{% endif %}
{% endfor %}
